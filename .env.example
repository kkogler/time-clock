# Server Configuration
PORT=3001
NODE_ENV=development

# Database Configuration
# In development: use local database or Railway dev environment
# In production: Railway will auto-inject this variable
DATABASE_URL="postgresql://user:password@localhost:5432/timeclock?schema=public"

# Optional: Add other environment variables as needed
# API_KEY=your_api_key_here
